import React, { Component } from 'react';

class Notificacion extends Component {
    static mostrar = (titulo, mensaje) => {
        //lo que hará será mandar un mensaje a los componentes de HTML para que sume alguna notificación
    }

}

export default Notificacion;